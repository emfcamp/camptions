reconnect of ingest/backend clears the frontend which it should not do
refreshing frontend shows history but then doesn't display the segments subseqently generated
create an admin toggle for each venue to disable transcription - this should display a suitable message on the viewer/display pages
restructure the main scroll arrangement on viewer.html - the header and footer currently sit under the main